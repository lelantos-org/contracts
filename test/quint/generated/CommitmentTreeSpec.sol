// SPDX-License-Identifier: Apache-2.0
pragma solidity 0.8.36;

// GENERATED by @lelantos-org/quint-sol-connect from spec/commitment_tree.qnt
// Do not edit. Regenerate with `quint-sol-connect gen commitmentTree`.
// Content hash: 0x879ec5bf3a5f6e89c195b12a7b0e605501a0826199d4c315426ca2730d374c91

library CommitmentTreeSpec {
    /// keccak256 of the canonical ABI type string for `Step[]`, and of the action
    /// names - which are not part of that string, but decide what each recorded
    /// `uint8` tag means. Asserted against every fixture's `meta.schemaHash` before
    /// decoding, so a config change not followed by a regeneration fails by name.
    bytes32 internal constant SCHEMA_HASH = 0xc667e7f7363eb8912cffe5aa37e03bdb351f496fa822e9db7d72764c83f9c0a9;

    /// (uint8,(bool,uint256,bool,uint256),(uint256[],uint256,uint256,uint256[]))[]
    string internal constant CANONICAL_TYPE =
        "(uint8,(bool,uint256,bool,uint256),(uint256[],uint256,uint256,uint256[]))[]";

    /// Order pins the enum values; it comes from the config's `actions` map.
    enum Action {
        Advance
    }

    /// One per Quint state variable, in config declaration order.
    /// Not compared: `advances` - ghost: counts advances so `inv_indexTracksAdvances` can catch a wrong ring stride, which no other invariant constrains
    /// Not compared: `insertedTotal` - ghost: sums `inserted` so `inv_countIsSumOfInserts` can catch counting advances instead of leaves
    struct State {
        uint256[] rootRing;
        uint256 rootIndex;
        uint256 committedCount;
        uint256[] knownRoots;
    }

    /// The union of every action's nondet picks. A step carries the whole
    /// struct whatever action it took, so `has*` distinguishes "not drawn
    /// this step" from "drawn, and the value was zero".
    struct Picks {
        bool hasNewRoot;
        uint256 newRoot;
        bool hasInserted;
        uint256 inserted;
    }

    struct Step {
        Action action;
        Picks picks;
        /// The model state *after* `action` ran.
        State post;
    }

    /// Names rather than indices, so a divergence reads as a value a human wrote.
    function actionName(Action v) internal pure returns (string memory) {
        return "advance";
    }
}
